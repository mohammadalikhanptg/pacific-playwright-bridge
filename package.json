{
  "name": "pacific-playwright-bridge",
  "version": "1.0.0",
  "description": "MCP server providing browser automation for the Pacific Assurance Dashboard via Playwright. Authenticates as qa@pacific.london (viewer role).",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "ts-node src/index.ts"
  },
  "dependencies": {
    "playwright": "^1.44.0"
  },
  "devDependencies": {
    "@types/node": "^20.12.0",
    "typescript": "^5.4.0",
    "ts-node": "^10.9.0"
  }
}
